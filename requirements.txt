typer[all]>=0.12
rich>=13
ffmpeg-python>=0.2
watchdog>=4
pydantic>=2
pysubs2>=1.7
mutagen>=1.47
faster-whisper>=1.0
requests>=2.31.0
pyacoustid>=1.1
musicbrainzngs>=0.7
yt-dlp>=2024.11.0
pytest>=9.0
mypy>=1.11
pre-commit>=4.0
types-requests>=2.32

# Optional heavy dependencies - install separately if needed:
# openai-whisper
# torch
# PySide6

# Optional: offline subtitle translation (GPU-accelerated)
# ctranslate2>=4.0
# transformers>=4.40
# sentencepiece>=0.2

# Optional: offline subtitle translation (CPU fallback, no CUDA required)
# argostranslate>=1.9

# Optional: automatic source language detection
# langdetect>=1.0.9